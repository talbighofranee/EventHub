{% extends 'base.html.twig' %}

{% block title %}Edit Reservation{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1 class="card-title">Edit Reservation</h1>
        </div>
        <div class="card-body">
            {{ include('reservation/_form.html.twig', {'button_label': 'Update'}) }}<br>
                        <a href="{{ path('app_reservation_index') }}" class="btn btn-secondary">Back to List</a><br>
                                    {{ include('reservation/_delete_form.html.twig') }}


        </div>
    </div>
{% endblock %}
